# backend/requirements.txt
# Sentinel — Backend Dependencies
# Install with: pip install -r requirements.txt

# ─────────────────────────────────────────
# Web Framework
# ─────────────────────────────────────────
fastapi

uvicorn[standard]==0.29.0          # ASGI server with websocket + http2 support
python-multipart==0.0.9            # Required for FastAPI form/file upload parsing

# ─────────────────────────────────────────
# Database
# ─────────────────────────────────────────
sqlalchemy==2.0.30                 # ORM
alembic==1.13.1                    # Schema migrations
psycopg2-binary==2.9.9             # PostgreSQL driver
asyncpg==0.29.0                    # Async PostgreSQL driver

# ─────────────────────────────────────────
# Redis
# ─────────────────────────────────────────
redis==5.0.4                       # Redis client
hiredis==2.3.2                     # C-accelerated Redis parser

# ─────────────────────────────────────────
# Authentication
# ─────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT encode/decode
passlib[bcrypt]==1.7.4             # Password hashing (bcrypt)

# ─────────────────────────────────────────
# Configuration
# ─────────────────────────────────────────
pydantic==2.7.1                    # Data validation (used by FastAPI)
pydantic-settings==2.2.1           # Settings management via .env
python-dotenv==1.0.1               # Load .env files

# ─────────────────────────────────────────
# NLP / ML Core
# ─────────────────────────────────────────
transformers==4.40.1         # HuggingFace Transformers (BERT, RoBERTa, etc.)
torch==2.3.0                 # PyTorch backend for HuggingFace models
tokenizers==0.19.1           # Fast tokenizers (used by transformers)
datasets==2.19.0             # HuggingFace datasets utility
accelerate==0.30.0           # Model loading optimization

spacy                        # Industrial NLP — NER, POS, dependency parsing
# Download model separately: python -m spacy download en_core_web_sm

scikit-learn==1.4.2                # ML utilities — classifiers, vectorizers, metrics
numpy==1.26.4                      # Numerical computing
scipy==1.13.0                      # Statistical functions (used in perplexity scoring)

# ─────────────────────────────────────────
# Computer Vision & Facial Recognition
# ─────────────────────────────────────────
deepface==0.0.90                   # Deep learning facial recognition
tf-keras==2.16.0                   # Required by deepface for model loading
opencv-python-headless==4.9.0.80   # Headless OpenCV for docker environments

# ─────────────────────────────────────────
# Text Analysis Utilities
# ─────────────────────────────────────────
nltk==3.8.1                        # Tokenization, stopwords, sentiment primitives
textblob==0.18.0                   # Lightweight sentiment + polarity scoring
rapidfuzz==3.9.0                   # Fast fuzzy string matching (company name spoofing)
tldextract==5.1.2                  # Reliable TLD/domain extraction from URLs
validators==0.28.3                 # URL and email format validation
chardet==5.2.0                     # Character encoding detection for input sanitization

# ─────────────────────────────────────────
# Rate Limiting
# ─────────────────────────────────────────
slowapi==0.1.9                     # Rate limiting middleware for FastAPI (Starlette-compatible)

# ─────────────────────────────────────────
# HTTP Client (for news source cross-referencing)
# ─────────────────────────────────────────
httpx==0.27.0                      # Async HTTP client for external API calls
beautifulsoup4==4.12.3             # HTML parsing for URL content fetching
lxml==5.2.1                        # Fast XML/HTML parser (BeautifulSoup backend)

# ─────────────────────────────────────────
# Testing
# ─────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6             # Async test support
httpx==0.27.0                      # Used as async test client for FastAPI
